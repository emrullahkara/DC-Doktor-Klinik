# Yerel geliştirme veritabanı. Rolleri ve veritabanını scripts/db-init.sql oluşturur.
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - ./scripts/db-init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
