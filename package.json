{
  "name": "dc-doktor-klinik",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "db:migrate": "pnpm --filter @dc/api db:migrate",
    "dev:api": "pnpm --filter @dc/api dev"
  },
  "engines": {
    "node": ">=22"
  }
}
